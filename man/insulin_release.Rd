% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insulin_release.R
\name{insulin_release}
\alias{insulin_release}
\title{Beta-Cell Insulin Release Indices}
\usage{
insulin_release(data, category = c("fasting", "ogtt"))
}
\arguments{
\item{data}{A data frame containing the columns required for the requested
\code{category} (see Details).}

\item{category}{Character vector selecting which indices to calculate.
One or both of \code{"fasting"} and \code{"ogtt"}. Defaults to both.}
}
\value{
The input data frame with the requested index columns appended.
}
\description{
Calculates surrogate indices of beta-cell insulin secretion from fasting
and oral glucose tolerance test (OGTT) glucose/insulin measurements.
}
\details{
Required columns per category:
\itemize{
\item \code{fasting}: \code{"G0"}, \code{"I0"}
\item \code{ogtt}: \code{"G0"}, \code{"I0"}, \code{"G30"}, \code{"I30"}, \code{"G120"}, \code{"I120"}, \code{"sex"}, \code{"bmi"}
}

Expected units: glucose (\code{G0}, \code{G30}, \code{G120}) in mmol/L, insulin (\code{I0},
\code{I30}, \code{I120}) in pmol/L, \code{bmi} in kg/m^2, and \code{sex} as a male indicator
(male = 1, female = 0). The BIGTT-AIR and BIGTT-SI terms enter \code{sex}
as a linear multiplier, so any value other than 1 is treated as female;
females coded as 2 are handled correctly. Internally, insulin is
converted from pmol/L to microU/mL (\code{value * 0.1667}) and glucose from
mmol/L to mg/dL (\code{value * 18}) where the source formula was derived in
those units.
If a required column is missing, the corresponding indices are skipped
with a warning; missing values within a present column yield \code{NA} only
for the indices that depend on them.

Indices calculated:
\itemize{
\item \code{Homa_beta} (\code{fasting}): HOMA-beta, an estimate of steady-state beta-cell
function.
\item \code{Cir} (\code{ogtt}): Corrected Insulin Response.
\item \code{Stumvoll} (\code{ogtt}): Stumvoll first-phase insulin release index.
\item \code{Xinsdg30} (\code{ogtt}): insulinogenic index using the 0-30 min glucose
increment.
\item \code{Xinsg30} (\code{ogtt}): insulinogenic index using the 30 min glucose value.
\item \code{Di} (\code{ogtt}): Disposition Index (\code{Xinsg30}-based).
\item \code{Bigtt_air} (\code{ogtt}): BIGTT Acute Insulin Response.
\item \code{Dibig} (\code{ogtt}): Disposition Index (\code{Bigtt_air}-based).
}
}
\examples{
data(example_data)
insulin_release(example_data, category = "fasting")
insulin_release(example_data, category = c("fasting", "ogtt"))

}
\references{
Madsen AL, et al. (2024). Genetic architecture of oral glucose-stimulated
insulin release provides biological insights into type 2 diabetes
aetiology. Nature Metabolism. \doi{10.1038/s42255-024-01140-6}
}
